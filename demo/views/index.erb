<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
  <head>
    <title>Syncshare test</title>
    <script type="text/javascript" src="easyXDM.debug.js"></script>
    <script type="text/javascript" src="client.js"></script>
  </head>
  <body>
    Welcome on the board <em> <%= current_user['name'] %> </em>
  </body>

  <script type="text/javascript">

    var session = Syncshare.connect('http://localhost:8080/syncshare/twitter', function(session) {

        session.on('upload', function(data, isBroadcast) {
            console.log('DATA', data, 'BROADCAST', isBroadcast);
        }).on('observe', function(data, isBroadcast) {
            console.log('DATA', data, 'BROADCAST', isBroadcast);
        });

    setTimeout(function() {
        session.call('upload', { abc: 1 });
    }, 3000);
    });
  </script>
</html>
